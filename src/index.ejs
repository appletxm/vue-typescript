<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width,minimum-scale=1.0,maximum-scale=1.0,user-scalable=no">
	<meta name="format-detection" content="telephone=no">
	<title>vue and typescript</title>
	<link rel="shortcut icon" href="assets/images/favicon.ico" />
	<% var cssFiles = htmlWebpackPlugin.options.libFiles.css %>
		<% for (var index in cssFiles) { %>
			<link href="<%= cssFiles[index] %>" rel="stylesheet" />
			<% } %>
</head>

<body>
	<div id="app" class="app"></div>

	<% var jsFiles = htmlWebpackPlugin.options.libFiles.js %>
		<% for (var index in jsFiles) { %>
			<script type="text/javascript" src="<%= htmlWebpackPlugin.options.publicPath + jsFiles[index] %>" charset="utf-8"></script>
			<% } %>

</body>

</html>
